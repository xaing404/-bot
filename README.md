# TedaBot — 微信多场景智能体机器人

基于 **wxauto4**（Windows UI 自动化）+ **OpenAI 兼容接口** 的微信群聊/私聊智能体。
支持多场景独立记忆、角色扮演、主动互动与群静默接话，不受网页协议限制，
任何可登录 PC 微信（4.x）的账号均可使用。

## 功能特性

### 消息处理

- 群聊/私聊消息监听：捕获内容、发送者、时间戳，双保险去重（id 指纹 + 内容指纹）

- 关键词触发：精确匹配 + 模糊匹配（相似度阈值可调）

- @机器人触发：消息属性 + 文本双重判定，享最高优先级，绕过回复冷却

- @提及精准过滤：AI 只看到纯文本问题（兼容微信 U+2005 特殊空格），
  不会把 `@机器人昵称` 误解析为对话对象；正文中的邮箱等 `@` 不受影响

- 多消息拆分发送：回复按换行拆为多条消息逐条发送（条间停顿可配），模拟真人连发

### 多场景记忆系统

- 场景键 `group:<群名>` / `private:<昵称>`，群聊与私聊记忆**完全隔离**（含同名情况）

- 场景记忆持久化：每场景一个 JSON 文件，原子写入，重启自动恢复

- 场景级防刷屏：私聊/群聊独立最小回复间隔，@回复、普通回复、主动发言共享冷却计时

- 并发安全：每场景一把线程锁（同场景串行、跨场景并发）

### 主动互动引擎

- 三种模式：`keyword` 只关键词回复 / `context` 只上下文主动 / `hybrid` 混合

- 三种触发策略（可叠加）：

  - **消息阈值**：每累计 N 条群消息后主动跟一句话题

  - **活跃度冒泡**：超过最大间隔且群里有新消息时冒泡

  - **群静默接话**：超过 `silence_interval` 无任何成员发言时，基于上下文自动接话；
    任一成员发新消息立即重置计时

- 质量保障：与近期内容相似度过高即丢弃、敏感词过滤、每日/每周发言上限

### 可靠性与可观测性

- **串行发送队列**：所有回复统一入队，专职线程按序发送（优先级：@回复 > 普通回复 > 主动发言），
  单条完整发完才发下一条，杜绝多回复乱序/穿插；异常隔离不阻塞队列

- **UI 互斥锁 + 窗口身份校验**：微信窗口的读写操作全程串行化，读取前校验当前窗口
  会话身份，杜绝主窗口模式下消息归属错乱（跨群状态污染）

- **发送看门狗**：单条消息发送超 30 秒自动告警，卡死可从日志立即定位

- **状态监控**：周期输出各活跃场景的记忆条数、回复次数、发送队列指标

- watchdog：主循环异常自动重启（连续崩溃 10 次停止）；退出前冲刷发送队列不丢消息

### Web 管理后台（Dashboard）

- **实时仪表盘**：活跃场景数、回复总数、发送队列指标、运行时长一目了然，5 秒自动刷新

- **场景记忆管理**：查看所有群聊/私聊场景的记忆条数、回复次数、最后活跃时间

- **角色卡浏览**：列出已配置的角色卡（内联/文件来源），标记默认角色

- **配置总览**：只读展示当前 trigger / proactive / ai 等配置段（API Key 自动脱敏）

- **实时日志流**：直接在浏览器查看机器人日志，按 INFO/WARN/ERROR 着色，3 秒自动刷新

- **系统健康**：CPU/内存占用、运行时长、Python 版本

- **布局方案对比**：三种界面布局方案的设计思路、优缺点与适用场景分析

- **零侵入**：Dashboard 在守护线程中运行，默认关闭，不影响机器人主循环；所有 API 只读

## 架构概览

```
                      ┌────────────────────────────────────────────┐
                      │                main.py                     │
                      │  轮询主循环 + 线程池(8) + watchdog 自动重启  │
                      └──────┬─────────────────────┬───────────────┘
              消息            │                     │ 回复入队
                              ▼                     ▼
   ┌──────────────┐   ┌──────────────┐   ┌──────────────────┐
   │ wechat_client│──▶│   handler    │   │    send_queue    │
   │ wxauto4 封装 │   │ 过滤→触发→AI │──▶│ 优先级FIFO串行发送│
   │ UI锁+身份校验│   │  场景锁/限流 │   │   看门狗告警     │
   └──────▲───────┘   └───┬──────┬───┘   └──────────────────┘
          │               │      │
          │        ┌──────▼──┐ ┌─▼────────────┐
   微信4.x │        │ai_backend│ │ proactive    │
   群/私聊 │        │ OpenAI兼容│ │ 主动互动引擎 │
          │        │ +上下文   │ │ 阈值/活跃度/ │
          │        └──────┬───┘ │ 群静默接话   │
          │               │     └──────┬───────┘
          │        ┌──────▼────────────▼───────┐
          └────────│  memory_store 场景记忆持久化│
                   └───────────────────────────┘
```

## 环境要求

| 项目        | 要求                                 |
| --------- | ---------------------------------- |
| 操作系统      | Windows 10/11                      |
| Python    | 3.9 - 3.12                         |
| 微信 PC 客户端 | **4.x**（wxauto4 免费版最高支持 4.1.8.107） |
| 网络        | 可访问所选 AI 接口                        |

> 注：微信已强制升级至 4.x，老的 wxauto（3.9.11 方案）已废弃，本项目使用 wxauto4。

## 快速开始

1. **克隆并安装依赖**

   ```powershell
   git clone https://github.com/xaing404/weixin-bot.git
   cd weixin-bot/teda_bot
   pip install -r requirements.txt
   ```

2. **创建配置文件**

   ```powershell
   Copy-Item config.example.yaml config.yaml
   ```

   编辑 `config.yaml`，至少填写：

   - `ai.base_url / api_key / model`：你的 AI 接口信息

   - `wechat.group_whitelist`：允许响应的群名（与微信显示**完全一致**）

   - `wechat.private_whitelist`：需要监听的私聊好友昵称

   - `bot.name` 可留空：自动读取你的微信昵称用于 @识别

3. **启动**

   ```powershell
   python .\main.py
   ```

4. **强烈建议：在微信中双击白名单群聊，打开独立聊天窗口**

   - 有独立窗口：机器人直接轮询该窗口，不干扰主窗口正常使用，
     消息归属、发送可靠性、响应速度均最优

   - 无独立窗口：机器人轮换切换主窗口拉取/发送消息（已做互斥锁与身份校验），
     但仍建议不要同时手动操作微信主窗口

## 配置说明

### 触发规则（trigger）

| 配置                          | 说明                     |
| --------------------------- | ---------------------- |
| `keywords`                  | 群聊关键词列表，命中即回复（私聊默认不受限） |
| `fuzzy` / `fuzzy_threshold` | 模糊匹配开关 / 相似度阈值（0\~1）   |
| `reply_on_at`               | 被 @ 时是否回复（最高优先级，绕过冷却）  |
| `reply_private`             | 私聊是否无需关键词直接回复          |
| `reply_at_sender`           | 群聊回复是否在开头 @提问人         |
| `duplicate_threshold`       | 与最近回复相似度 ≥ 该值时自动拦截     |

### 主动互动（proactive）

| 配置                              | 说明                         |
| ------------------------------- | -------------------------- |
| `mode`                          | keyword / context / hybrid |
| `message_threshold`             | 每累计多少条群消息后主动发言一次           |
| `min_interval` / `max_interval` | 两次发言最小间隔（防刷屏）/ 活跃度冒泡最大间隔   |
| `silence_interval`              | 群静默接话时长（秒），无成员发言超时即接话；0 关闭 |
| `context_window`                | 参与上下文分析的最近消息条数             |
| `daily_cap` / `weekly_cap`      | 每日 / 每周主动发言上限              |
| `repeat_threshold`              | 与近期内容相似度超过该值则放弃（防重复）       |

### AI 接口（ai）

| 配置                               | 说明                                           |
| -------------------------------- | -------------------------------------------- |
| `base_url` / `api_key` / `model` | OpenAI 兼容接口信息（OpenAI/DeepSeek/本地代理/ollama 等） |
| `max_context_rounds`             | 每会话保留的最大对话轮数                                 |
| `context_max_age`                | 上下文时间边界（秒），更早的历史不再带入 AI                      |
| `request_interval`               | 两次 AI 请求最小间隔（秒）                              |
| `timeout`                        | 单次 AI 请求超时（秒）                                |
| `max_retries` / `retry_backoff`  | 重试次数 / 指数退避基数                                |

### 微信与发送（wechat）

| 配置                                      | 说明                     |
| --------------------------------------- | ---------------------- |
| `poll_interval`                         | 消息轮询间隔（秒）              |
| `dedup_window`                          | 消息去重时间窗（秒）             |
| `send_queue_size`                       | 发送队列容量，满时丢弃新消息并告警      |
| `multi_message_delay`                   | 多条消息连发的条间停顿（秒），0 无停顿   |
| `group_whitelist` / `private_whitelist` | 监听的群聊 / 私聊白名单（名称须完全一致） |

### 其他

| 配置                                     | 说明                          |
| -------------------------------------- | --------------------------- |
| `reply_interval.private` / `group`     | 场景级最小回复间隔（秒），防刷屏            |
| `memory.enabled` / `dir`               | 场景记忆持久化开关 / 存储目录            |
| `roles.default` / `roles.cards`        | 默认角色卡名 / 角色卡定义（内联或 JSON 文件） |
| `safety.enabled` / `block_words`       | 敏感词过滤开关 / 词表                |
| `logging.level` / `file` / `max_bytes` | 日志级别 / 路径 / 滚动大小            |

### Web 管理后台（dashboard）

| 配置          | 说明                                        |
| ----------- | ----------------------------------------- |
| `enabled`   | 是否启用 Dashboard（默认 `false`，在守护线程中运行不影响主循环） |
| `host`      | 监听地址（`127.0.0.1` 仅本机访问；`0.0.0.0` 允许局域网访问） |
| `port`      | 端口（默认 `8050`）                             |
| `log_lines` | 日志 API 返回的最近行数（默认 `200`）                  |

## 使用指南

### 触发方式一览

| 场景 | 触发方式   | 说明                            |
| -- | ------ | ----------------------------- |
| 群聊 | @机器人   | 最高优先级，立即回复并 @提问人              |
| 群聊 | 消息含关键词 | 精确/模糊匹配，受场景冷却限制               |
| 群聊 | 主动互动   | 阈值/活跃度/群静默三种策略自动触发            |
| 私聊 | 任意消息   | 默认直接回复（`reply_private: true`） |

### 角色卡

支持两种定义方式，`{bot_name}` 替换为机器人昵称，`{{user}}` 替换为发送者昵称：

```yaml
roles:
  default: "assistant"
  cards:
    assistant:                       # 方式一：内联定义
      name: "通用助手"
      prompt: "你是 {bot_name}，一个友好、简洁的中文聊天助手。"
    yandere:                         # 方式二：JSON 角色卡文件
      file: "角色卡_病娇.json"
```

JSON 角色卡兼容中英文键名（`name/角色名称`、`prompt/系统提示词`），
结构化角色卡（无整体提示词）会自动合成兜底 prompt（上限 5000 字）。

### 场景记忆

记忆文件按场景存储于 `memory/` 目录（已从 Git 排除，保护隐私）：

```
memory/
├── group_谁也没我们.json    # 键为 group:谁也没我们
├── private_张三.json        # 键为 private:张三
└── ...
```

删除对应文件即可清空某场景的记忆；关闭 `memory.enabled` 则仅存内存。

## Web 管理后台使用

### 启用 Dashboard

在 `config.yaml` 中添加：

```yaml
dashboard:
  enabled: true
  host: "127.0.0.1"
  port: 8050
```

正常启动机器人即可：

```powershell
python .\main.py
```

日志中出现 `Dashboard 已启动: http://127.0.0.1:8050` 后，浏览器打开该地址即可访问管理后台。

### 页面说明

| 页面    | 功能                                                | 数据刷新  |
| ----- | ------------------------------------------------- | ----- |
| 仪表盘   | KPI 总览（活跃场景/回复数/队列指标/运行时长）、活跃场景列表、发送队列状态、活动时间线    | 5 秒自动 |
| 场景记忆  | 全部场景列表（群聊/私聊），含记忆条数、回复次数、最后活跃时间                   | 手动刷新  |
| 角色卡管理 | 已配置角色卡展示（名称/来源/默认标记），支持内联与 JSON 文件两种来源            | 手动刷新  |
| 触发规则  | 当前触发配置只读展示：关键词列表、模糊匹配开关、相似度阈值等                    | 手动刷新  |
| 主动互动  | 当前主动互动配置：模式（keyword/context/hybrid）、发言间隔、频率上限     | 手动刷新  |
| 日志监控  | 实时日志流（按 INFO/WARN/ERROR/TASK 着色），发送队列/场景状态/系统健康面板 | 3 秒自动 |
| 布局方案  | 三种界面布局方案对比：经典侧边栏/顶部导航卡片/双栏分区，含优缺点分析               | 静态    |

### REST API

所有 API 为只读，不修改运行时状态：

| 端点                  | 方法  | 说明                           |
| ------------------- | --- | ---------------------------- |
| `/api/stats`        | GET | 仪表盘 KPI：场景数、回复数、队列指标、运行时长    |
| `/api/scenarios`    | GET | 场景列表：键、类型、名称、记忆条数、回复数、最后活跃   |
| `/api/roles`        | GET | 角色卡列表：ID、名称、是否默认、来源          |
| `/api/config`       | GET | 当前配置只读展示（API Key 自动脱敏）       |
| `/api/logs?lines=N` | GET | 最近 N 行日志（默认 200），每行含级别与时间戳   |
| `/api/health`       | GET | 系统健康：CPU/内存占用、Python 版本、运行时长 |

### 技术架构

- **后端**：Flask 3.x，在 `main.py` 的 `run_once()` 中以守护线程启动，与消息轮询主循环并行

- **前端**：7 个静态 HTML 页面（Anodized Deep 深色主题），Tailwind CSS + Lucide 图标

- **数据对接**：`dashboard-api.js` 在前端定时轮询 `/api/*` 端点，自动更新 DOM

- **安全**：API Key 在 `/api/config` 中自动脱敏；默认仅监听 `127.0.0.1`

- **依赖**：新增 `flask>=3.0`（Dashboard 关闭时无需安装）

### 界面布局方案

Dashboard 提供三种布局方案对比（在"布局方案"页面查看完整分析）：

1. **经典侧边栏布局**（当前方案）：240px 固定侧边栏 + 56px 顶栏 + 主内容区。导航始终可见，空间利用率高，适合功能模块多的场景。
2. **顶部导航卡片布局**：取消侧边栏，顶部水平导航 + 卡片网格。最大化内容宽度，适合功能模块少的场景。
3. **双栏分区布局**：可折叠左栏 + 右侧操作区/预览区。适合需要同时操作和监控的复杂场景。

## 目录结构

```
teda_bot/
├── main.py                # 入口：轮询主循环 + 线程池 + watchdog 自动重启 + 可选 Dashboard
├── config.yaml            # 本地配置（不入库），config.example.yaml 为模板
├── requirements.txt
├── bot/
│   ├── wechat_client.py   # wxauto4 封装：监听/发送/UI互斥锁/窗口身份校验
│   ├── handler.py         # 消息管线：过滤→触发→AI→回复（场景锁/去重/限流）
│   ├── send_queue.py      # 串行发送队列：优先级FIFO + 看门狗
│   ├── proactive.py       # 主动互动引擎：消息阈值/活跃度/群静默接话
│   ├── memory_store.py    # 场景记忆持久化（原子写入 JSON）
│   ├── matcher.py         # 关键词匹配 + @提及精准过滤
│   ├── role_cards.py      # 角色卡系统（中英文键名兼容）
│   ├── ai_backend.py      # OpenAI 兼容接口 + 多场景上下文管理
│   ├── rate_limit.py      # 场景级频率限制
│   ├── dedup.py           # 消息双保险去重
│   ├── safety.py          # 敏感词过滤
│   └── logger.py          # 滚动日志
├── dashboard/             # Web 管理后台（Flask 守护线程）
│   ├── __init__.py        # 包入口
│   ├── state.py           # 线程安全的运行时状态容器
│   └── server.py          # Flask 应用：页面托管 + REST API
├── tedabot-dashboard/     # 前端页面资源（Anodized Deep 深色主题）
│   ├── pages/             # 7 个 HTML 页面
│   │   ├── dashboard.html # 仪表盘：KPI 总览 + 队列 + 场景 + 时间线
│   │   ├── scenarios.html # 场景记忆：数据表 + 过滤
│   │   ├── roles.html     # 角色卡管理：卡片网格
│   │   ├── trigger.html   # 触发规则：分段配置表单
│   │   ├── proactive.html # 主动互动：模式选择 + 阈值/频率配置
│   │   ├── logs.html      # 日志监控：实时日志流 + 状态面板
│   │   └── layouts.html   # 布局方案：三方案对比分析
│   ├── assets/            # 前端 JS（dashboard-api.js 数据对接层）
│   └── colors_and_type.css# Anodized Deep 设计令牌
└── tests/                 # pytest 单元测试（160 项：139 业务 + 21 Dashboard）
```

## 测试

```powershell
python -m pytest tests/ -v
```

测试覆盖 160 项：139 项业务逻辑测试（消息处理/触发/记忆/队列/角色卡/安全等）+ 21 项 Dashboard 测试（API 端点/状态容器/工具方法）。

## 常见问题排查

| 现象              | 排查方向                                                                          |
| --------------- | ----------------------------------------------------------------------------- |
| 启动报无法连接微信窗口     | 微信 4.x 已登录且**主窗口已打开**（不能只挂在托盘）                                                |
| 不回复             | 检查群名/昵称与微信显示完全一致；消息需含关键词或 @机器人；查日志「捕获消息」                                      |
| 回复答非所问          | 检查上下文是否被陈旧话题污染（可删 `memory/` 对应场景文件重置）；`ai.context_max_age` 调小                 |
| 消息发送卡住          | 日志出现「发送疑似卡住」时，优先为监听群**打开独立聊天窗口**；确认微信窗口未被最小化                                  |
| 群串台/归属错乱        | 日志出现「窗口切换校验失败」即为拦截记录；建议打开独立窗口根治                                               |
| AI 频繁超时         | 上游接口慢：调大 `ai.timeout`、减小 `max_retries`，或更换接口                                  |
| 主动发言太频繁         | 调大 `message_threshold`/`min_interval`，或 `silence_interval` 设 0                |
| 程序反复重启          | 查看日志末尾异常栈；连续崩溃 10 次会自动停止                                                      |
| Dashboard 无法访问  | 确认 `config.yaml` 中 `dashboard.enabled: true`；安装 `pip install flask`；检查端口是否被占用 |
| Dashboard 数据不刷新 | 机器人需正常运行（非仅启动 Dashboard）；API 不可用时页面保留设计稿数据                                    |

## 贡献指南

欢迎提交 Issue 与 Pull Request：

1. Fork 仓库并创建功能分支：`git checkout -b feature/your-feature`
2. 提交前运行测试确保通过：`python -m pytest tests/ -v`
3. 遵循现有代码风格（中文注释、模块级 docstring 说明设计意图）
4. 提交信息使用约定式格式（`feat:` / `fix:` / `docs:` / `refactor:`），说明"为什么"而非仅"改了什么"

## 许可证

本项目采用 [MIT License](LICENSE) 开源。

## 免责声明

- wxauto4 基于 UIAutomation，仅供学习交流，请勿用于生产/商业用途

- 自动回复行为请遵守微信用户协议，高频滥用有账号风控风险

- 免费版 wxauto4 无后台监听，采用轮询模式；微信客户端升级超过 4.1.8.107 后可能失效

- 请勿将本项目用于垃圾信息群发、骚扰等违规场景，使用者自行承担相应责任

